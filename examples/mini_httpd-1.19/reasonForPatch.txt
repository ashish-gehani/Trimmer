Function getline in htpasswd.c contradicts with stdio.h getline function so the name of the function is changed from getline to getline1; 
otherwise there will be compilation error.
A loop in function read_config of mini_httpd.c is not unrolled by llvm (due to limitations), so the loop is converted into an equivalent loop, 
which can be unrolled by llvm.
